{
  "name": "cvbuilder-monorepo",
  "version": "1.2.2",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run start --workspace=frontend",
    "build": "npm run build --workspace=frontend"
  },
  "engines": {
    "node": "20.x"
  }
}
